name: estado
description: A flutter agnostic implementation of MVVM. It will leave your Widgets alone and you can focus on writing clean Flutter code
version: 0.0.9
homepage: https://github.com/MrIceman/estado

environment:
  sdk: ">=2.17.1 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
